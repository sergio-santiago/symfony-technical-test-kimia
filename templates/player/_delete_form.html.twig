<form method="post" action="{{ path('player_delete', {'id': player.id}) }}" onsubmit="return confirm('Are you sure you want to delete this player?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ player.id) }}">
    <button class="btn">Delete</button>
</form>
